import React from "react";
import {SceneShell} from "../shared";
export const HistoryScene = () => <SceneShell index="07" kicker="Private scan ledger" title="Your analysis history stays in your browser session." body="Reopen prior results without exposing the scanned address list to other visitors. Every report keeps its model source, confidence, limitations, findings, and evidence."><div style={{display:"grid",gap:12,maxWidth:1120}}>{[["01","ERC-4626 VAULT","ANALYZED"],["02","CUSTOM SMART CONTRACT","ANALYZED"],["03","ERC-20 TOKEN","ANALYZED"]].map(([n,name,state])=><div key={n} style={{display:"grid",gridTemplateColumns:"80px 1fr 180px",alignItems:"center",padding:"22px 26px",border:"2px solid #111315",fontSize:26,fontWeight:900,background:"#fffaf0"}}><span>{n}</span><span>{name}</span><span style={{color:"#167a42"}}>{state}</span></div>)}</div></SceneShell>;
